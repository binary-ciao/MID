# 硕士毕业设计项目全局上下文文档

## 1. 项目基本信息
* **课题名称**：《基于大模型意图认知与扩散生成的复杂场景轨迹预测》
* **学科专业**：计算机科学与技术
* **应用场景**：复杂高危工业场景（如油气勘探生产场站、海上平台等），强调严格的物理约束与主动安全预警。
* **核心难点**：打通离散语义认知（意图提取）与连续物理空间（轨迹坐标生成）之间的跨模态信息壁垒。

## 2. 研究背景与核心痛点
* **痛点 1（缺乏意图理解）**：传统的深度学习轨迹预测方法（如 Transformer, LSTM）仅在低维数值坐标空间进行黑盒回归，难以理解工人深层次的动机（是"违规逗留"还是"紧急处置"）。
* **痛点 2（无视物理约束）**：现有生成模型极易生成穿越障碍物（如穿墙、穿过油罐）的"非物理"危险轨迹。
* **痛点 3（多模态干扰）**：若前端意图识别出错（认知噪声），直接传入下游生成模型会导致预测发生灾难性偏移。

## 3. 总体技术路线 (三阶段架构)
本项目打破了传统的"数值坐标回归"范式，构建了 **"意图大脑" + "跨模态桥梁" + "生成躯干"** 的预测框架：
1. **认知大脑 (The Brain)**：利用多模态大语言模型（MLLM）对监控视频进行意图识别。
2. **映射桥梁 (The Bridge)**：设计跨注意力机制（Cross-Attention）或软门控（Soft Gating），将文本意图向量与视觉/轨迹时空特征进行对齐与动态融合。
3. **生成躯干 (The Body)**：以扩散模型（Diffusion Model）为引擎，将融合后的特征作为条件，结合物理能量函数（约束梯度），引导生成符合工业安全规则的宏观意图轨迹。

## 4. 关键技术与实现细节
### 4.1 数据集与预处理工程
* **数据集来源**：VIRAT Video Dataset（提取以 `Person_` 开头的行人和车辆交互动作）。
* **数据清洗原则**：严格执行"单目标聚焦"与"视频级隔离"。
* **预处理技术**：利用 Python `multiprocessing` 多进程结合 OpenCV，解析 `.geom.yml` 与 `.activities.yml`，动态提取核心动作的短视频片段（约5秒/帧率均匀采样）。
* **视觉提示工程 (Visual Prompt)**：在视频帧中对目标行人精准绘制**红色 Bounding Box**，强行锁定大模型注意力。

### 4.2 意图识别多模态大模型 (MLLM)
* **模型选型**：弃用 Video-LLaVA，全面切换至 **Qwen2-VL (2B版本用于敏捷开发，后期视需求冲刺4B/7B)**，利用其原生的视频抽帧支持和强大的 3D-RoPE（三维位置编码）时空感知能力。
* **输出结构化设计**：为防止特征稀释，强制大模型输出极简的三段式伪代码格式：
  `Motion: [..]. Status: [..]. Intent: [..].`
* **微调方案**：采用 LoRA 技术进行指令微调。

### 4.3 跨模态融合与扩散模型 (Diffusion)
* **意图-轨迹一致性感知 (动态无分类器引导 Dynamic CFG)**：
  针对 MLLM 可能出现的意图识别错误，在 Diffusion 采样中引入动态权重 $w$。若当前轨迹趋势与文本意图背离度高（置信度低），则动态降低意图文本的引导权重，回退至基于物理惯性的安全预测模式。
* **物理约束注入**：构建场景安全能量函数，通过梯度反向修正噪声，避免预测坐标落入禁区。

## 5. 已解决的技术难点记录
1. **多目标注意力混淆**：前期 MLLM 易出现"张冠李戴"，已通过 OpenCV 单目标红框渲染 + Prompt 显式锁定(`marked by the red bounding box`) 完美解决。
2. **预处理 CPU I/O 瓶颈**：单线程处理视频极慢，已重构为进程池（ProcessPoolExecutor）并发架构，效率提升十倍以上。
3. **MLLM 输入兼容性问题**：Video-LLaVA 存在张量维度限制和多帧封装难度，已果断迁移至 Qwen2-VL 体系。

## 6. 目前项目进度
* [x] 完成开题报告论证（专家组建议进一步凝练研究内容、细化跨模态映射机制）。
* [x] 跑通 VIRAT 数据集的多进程预处理闭环（生成红框视频切片与微调 JSON 字典）。
* [x] 完成 Qwen2-VL-2B 模型的 LoRA 微调。
* [x] 构建了格式遵循率与关键词命中率的评测脚本。
* [x] **现状评估**：微调后模型输出格式已 100% 达标，但核心意图识别准确率仍未达完美预期，确认存在"模态噪声"隐患。

## 7. 未来开发计划与待办事项 (TODO)
1. **下游扩散模型开发**：搭建 Diffusion 预测基座，设计将 Qwen2-VL 输出的结构化意图文本转化为 Text Embedding 的接入接口。
2. **鲁棒性融合机制编码**：在 Diffusion 的条件注入环节，通过代码实现"动态 CFG (Dynamic CFG)"或"Cross-Attention 软门控"，抑制错误意图文本的干扰。
3. **混合数据集构建**：利用 UE5 (Unreal Engine 5) 搭建高保真数字孪生环境，结合视频生成大模型，补充极端危险场景的合成数据，形成混合验证集。
4. **全链路联调与消融实验**：打通 `视频 -> MLLM意图提取 -> 动态门控融合 -> 扩散轨迹生成` 的完整前向传播链路，并设计多维度的 Baseline 对比实验。